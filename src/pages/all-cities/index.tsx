import React, { FC } from "react";
import { Header } from "src/components/Header";

const AllCities: FC = () => (
  <main>
    <Header />
    <h2>All Cities</h2>
  </main>
);

export default AllCities;
